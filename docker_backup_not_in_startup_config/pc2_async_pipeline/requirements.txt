# PC2 Async Pipeline Requirements - Optimized for auto-detected imports
# Core messaging and async
pyzmq==25.1.2
PyYAML==6.0.1

# System monitoring
psutil==5.9.6

# Deep learning (detected by auto scan)
torch==2.1.0

# Core async infrastructure
redis==5.0.1
nats-py==2.7.2
aiohttp==3.9.1

# Task scheduling and routing (async pipeline specific)
celery==5.3.4
schedule==1.2.0
apscheduler==3.10.4

# Python built-ins (sqlite3 is built-in, dataclasses in Python 3.7+)
# Note: pc2_code is internal module, not external package
