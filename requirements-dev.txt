# Python Code Formatting and Linting
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.0.0
pre-commit>=3.0.0

# For JSON/YAML formatting (if needed)
# prettier (install via npm/yarn if you want to format JSON/YAML/MD files) 