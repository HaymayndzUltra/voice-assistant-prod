# AI System Unified Configuration v3.0
# Single Source of Truth for All Machines and Environments
# Created: 2025-01-16
# Description: Consolidated configuration for MainPC (RTX 4090) and PC2 (RTX 3060) with Docker support

version: 3.0

# Placeholder v3 config automatically generated; currently forwards to legacy
# files to maintain backward compatibility.  The UnifiedConfigLoader will load
# this file first, then merge any machine-specific overrides found under
# config/overrides/.

include:
  - main_pc_code/config/startup_config.yaml
  - pc2_code/config/startup_config.yaml 