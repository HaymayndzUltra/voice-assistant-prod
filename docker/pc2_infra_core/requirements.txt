# PC2 Infrastructure Core Requirements - Optimized for auto-detected imports
# Core async and messaging
fastapi==0.104.1
uvicorn==0.24.0
redis==4.6.0
nats-py==2.5.0

# ZMQ and monitoring
pyzmq==25.1.2
prometheus-client==0.19.0
psutil==5.9.6

# Network and HTTP
requests==2.31.0
aiohttp==3.9.1

# Data handling
PyYAML==6.0.1
numpy==1.24.4

# ML/GPU support (detected by auto scan)
torch==2.1.0

# Python built-ins (sqlite3 is built-in, dataclasses in Python 3.7+)
# Note: pc2_code is internal module, not external package
