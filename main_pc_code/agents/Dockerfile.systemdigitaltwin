# syntax=docker/dockerfile:1.7

ARG BASE_IMAGE
FROM ${BASE_IMAGE}

WORKDIR /app

# Install only the minimal dependencies required by SystemDigitalTwin
COPY main_pc_code/agents/requirements.systemdigitaltwin.txt /app/requirements.txt
RUN python -m pip install --upgrade pip setuptools wheel \
    && pip install --no-cache-dir -r /app/requirements.txt

COPY main_pc_code/agents/ /app/agents/
ENV PYTHONPATH=/app

HEALTHCHECK --interval=30s --timeout=5s --start-period=20s --retries=5 \
  CMD ["python","-c","import importlib,sys; importlib.import_module('main_pc_code.agents.system_digital_twin'); sys.exit(0)"]

CMD ["python", "-u", "/app/agents/system_digital_twin.py"]


